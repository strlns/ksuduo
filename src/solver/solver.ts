import {Sudoku} from "../model/Sudoku";

/**
 * This function should either return the solved sudoku, or throw an error
 * if it is not solvable.
 * @param sudoku
 */
export default function solve(sudoku: Sudoku): Sudoku {
    throw new Error('Not implemented yet.');
}